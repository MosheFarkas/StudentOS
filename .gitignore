node_modules/
dist/
build/
*.tsbuildinfo

.env
.env.*
!.env.example

.DS_Store
*.log
npm-debug.log*
pnpm-debug.log*

coverage/
.pgdata/

# Output of the desktop app's `record` and `explore` commands. These are
# captures from a real student's portal, not source.
*-endpoints.json
*-map.json

# electron-builder output
release/

# ...but the desktop app's build resources ARE source: electron-builder reads
# the icon from here at package time.
!apps/desktop/build/
